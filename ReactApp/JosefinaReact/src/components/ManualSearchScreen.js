import React from 'react';
import { Text, View } from 'react-native';

export default class ManualSearchScreen extends React.Component {
  static navigationOptions = {
    title: 'Manual search',
  };
  render() {
    return (
      <View>
        <Text>Email</Text>
      </View>
    );
  }
}