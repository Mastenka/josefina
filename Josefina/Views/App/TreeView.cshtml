
<div class="jumbotron" style="min-height:250px; min-width:250px; margin:10px">
    <ul>
        <li ng-repeat="task in tasks">
            <div>{{task.Name}}</div>
            <div>{{task.TaskID}}</div>
        </li>
    </ul>
</div>